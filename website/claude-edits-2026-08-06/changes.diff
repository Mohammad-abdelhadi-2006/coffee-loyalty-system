diff --- index.css ---
--- before/index.css
+++ src/index.css
@@ -7,15 +7,17 @@
   --text-on-light: #7A6255;
   --text-on-dark: #F0E4D8;
   --fotter-bg: #5C3D2E;
-  --white:#FFFFFF;
+
   /*----------------------padding/margin----------------------*/
   --gap-sm: 0.875rem;
   --gap1: 1rem;
   --gap-md: 1.5rem;
   --gap-lg: 2.5rem;
-  --text-margin: auto;
-
 
+  /*----------------------font sizes----------------------*/
+  --font-sm: 1.05rem;
+  --font-md: 1.2rem;
+  --font-lg: 1.5rem;
 }
 
 * {
@@ -32,7 +34,7 @@
 /*----------------------header----------------------*/
 
 header {
-  background-color: var(--light-broun);
+  background-color: var(--broun);
   display: flex;
   align-items: center;
   justify-content: space-between;
@@ -40,6 +42,7 @@
   box-shadow: 0 2px 8px rgba(51, 32, 15, 0.12);
   position: sticky;
   top: 0;
+  z-index: 10;
 }
 
 header nav {
@@ -54,14 +57,16 @@
   text-decoration: none;
   color: var(--text-on-dark);
   padding: var(--gap-sm);
-  font-size: var(--gap-md);
+  font-size: var(--font-lg);
   display: flex;
   justify-content: center;
 }
 
-header h1 {
+header .logo {
   margin: var(--gap-sm);
   color: var(--text-on-dark);
+  font-size: 2rem;
+  font-weight: bold;
 }
 
 /*----------------------footer----------------------*/
@@ -96,8 +101,8 @@
 
 #root {
   min-height: 100dvh;
-  display: grid;
-  grid-template-rows: auto 1fr auto;
+  display: flex;
+  flex-direction: column;
 }
 
 .footer-inner>div {
@@ -123,23 +128,29 @@
 
 /*----------------------hero----------------------*/
 #hero {
+  flex: 1;
   display: flex;
   flex-wrap: wrap;
   align-items: center;
   gap: var(--gap-lg);
   padding: var(--gap-lg);
-  background-color: var(--light-broun);
-    font-size:var(--gap-md);
-
+  background-color: var(--broun);
+  font-size: var(--font-lg);
 }
 
 #hero>div {
   flex: 1 1 320px;
 }
 
+#hero h1 {
+  font-size: clamp(1.8rem, 5vw, 3rem);
+  line-height: 1.3;
+}
+
 #hero img {
   display: block;
-  width: 60%;
+  width: 100%;
+  max-width: 420px;
   margin-inline: auto;
   border-radius: 17px;
 }
@@ -160,37 +171,40 @@
   gap: var(--gap1);
 }
 
-.hero-btn button{
-  font-size:large;
+.hero-btn button {
+  font-size: var(--font-md);
+  font-family: inherit;
   padding: var(--gap-sm) var(--gap-md);
   border-radius: 8px;
+  cursor: pointer;
+  transition: background-color 0.2s, color 0.2s;
 }
 
-#menubtn {
+.btn-primary {
   background-color: var(--text-on-dark);
   border: 2px solid var(--text-on-dark);
   color: var(--dark-broun);
-  cursor: pointer;
-    transition: background-color 0.2s, color 0.2s;
-
 }
 
-#menubtn:hover {
+.btn-primary:hover {
   background-color: var(--light-broun);
   color: var(--text-on-dark);
-  
 }
 
-#location-btn {
+.btn-outline {
   background-color: transparent;
   border: 2px solid var(--text-on-dark);
   color: var(--text-on-dark);
-    cursor: pointer;
-
-  transition: background-color 0.2s, color 0.2s;
 }
 
-#location-btn:hover {
+.btn-outline:hover {
   background-color: var(--text-on-dark);
   color: var(--dark-broun);
+}
+
+/*----------------------responsive----------------------*/
+@media (max-width: 700px) {
+  #hero-text {
+    margin-inline-end: 0;
+  }
 }
\ No newline at end of file

diff --- Home.jsx ---
--- before/Home.jsx
+++ src/Home.jsx
@@ -4,15 +4,15 @@
   return (
     <section id="hero">
       <div>
-        <img src={HeroPic} alt="" />
+        <img src={HeroPic} alt="فنجان قهوة ساخنة بجانب حبوب البن" />
       </div>
       <div id="hero-text">
-      <h1>قهوة طازجة لكل مزاج</h1>
-      <p>نكهات غنية وأجواء دافئة كل يوم.</p>
-      <div className="hero-btn">
-  <button id='menubtn'>المنيو</button>
-  <button id='location-btn'>موقعنا</button>
-</div>
+        <h1>قهوة طازجة لكل مزاج</h1>
+        <p>نكهات غنية وأجواء دافئة كل يوم.</p>
+        <div className="hero-btn">
+          <button className="btn-primary">المنيو</button>
+          <button className="btn-outline">موقعنا</button>
+        </div>
       </div>
     </section>
 

diff --- headerFooter.jsx ---
--- before/headerFooter.jsx
+++ src/headerFooter.jsx
@@ -2,7 +2,7 @@
 function Header() {
   return (
     <header>
-      <h1>نكهة فنجان</h1>
+      <span className="logo">نكهة فنجان</span>
       <nav>
         <a href="#hero">الرئيسية</a>
         <a href="#menu">المنيو</a>

